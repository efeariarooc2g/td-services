/*const reindex = () => {
  console.log('Reindexing...');
  ESMongoSync.reIndex();
};

SyncedCron.add({
  name: 'Reindexing',
  schedule (parser) {
    return parser.text(SearchService.reindexInterval);
  },
  job () {
    reindex();
  }
});*/
